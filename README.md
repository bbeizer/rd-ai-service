# rd-ai-service

> **Note:** The minimax AI has been migrated to the Node.js backend. This repo is being repurposed for ML-based AI experimentation.

The minimax implementation is preserved here as a baseline for comparing against future ML models.

## Legacy Code Reference

- `ai_logic.py` - Minimax with alpha-beta pruning (depth 3)
- `game_logic.py` - Game rules, pass validation, win detection
- `utils.py` - Move generation, position utilities


